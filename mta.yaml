_schema-version: 3.3.0
ID: Customer_Order_Management
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: Customer_Order_Management-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: Customer_Order_Management-db
      - name: Customer_Order_Management-destination
      - name: Customer_Order_Management-auth

  - name: Customer_Order_Management-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: Customer_Order_Management-db
  - name: Customer_Order_Management
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: Customer_Order_Management-html5-runtime
      - name: Customer_Order_Management-auth
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  - name: Customer_Order_Management-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: Customer_Order_Managementfinance_dashboard
          artifacts:
            - finance_dashboard.zip
          target-path: app/
        - name: Customer_Order_Managementinvoice_analytics
          artifacts:
            - invoice_analytics.zip
          target-path: app/
        - name: Customer_Order_Managementsales_portal
          artifacts:
            - sales_portal.zip
          target-path: app/
    requires:
      - name: srv-api
      - name: Customer_Order_Management-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
      - name: Customer_Order_Management-auth
    parameters:
      config:
        destinations:
          - Name: srv-api
            URL: ~{srv-api/srv-url}
            Authentication: NoAuthentication
            Type: HTTP
            ProxyType: Internet
            HTML5.ForwardAuthToken: true
            HTML5.DynamicDestination: true

  - name: Customer_Order_Managementfinance_dashboard
    type: html5
    path: app/finance_dashboard
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: Customer_Order_Managementinvoice_analytics
    type: html5
    path: app/invoice_analytics
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: Customer_Order_Managementsales_portal
    type: html5
    path: app/sales_portal
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: Customer_Order_Management-destinations
    type: com.sap.application.content
    requires:
      - name: Customer_Order_Management-html5-repo-host
        parameters:
          service-key:
            name: Customer_Order_Management-html5-repo-host-key
      - name: srv-api
      - name: Customer_Order_Management-destination
        parameters:
          content-target: true
      - name: Customer_Order_Management-auth
        parameters:
          service-key:
            name: Customer_Order_Management-auth-key
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: Customer_Order_Management-html5-repository
              ServiceInstanceName: Customer_Order_Management-html5-repo-host
              ServiceKeyName: Customer_Order_Management-html5-repo-host-key
              sap.cloud.service: Customer_Order_Management.service
            - Name: Customer_Order_Management-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: Customer_Order_Management-auth
              ServiceKeyName: Customer_Order_Management-auth-key
              sap.cloud.service: Customer_Order_Management.service

resources:
  - name: Customer_Order_Management-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: Customer_Order_Management-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
    requires:
      - name: srv-api

  - name: Customer_Order_Management-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: Customer_Order_Management-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
  - name: Customer_Order_Management-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: Customer_Order_Management-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'SalesRep (Customer_Order_Management ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.SalesRep'
          - name: 'SalesManager (Customer_Order_Management ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.SalesManager'
          - name: 'Finance (Customer_Order_Management ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Finance'
          - name: 'Administrator (Customer_Order_Management ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Administrator'
    requires:
      - name: app-api
